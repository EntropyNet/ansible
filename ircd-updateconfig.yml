---
- hosts: ircdnodes
  user: ircd
  gather_facts: no
  tasks:
  - name: push new configs to all ircd nodes
    action: shell cd ~/entropynet && svn up
    notify:
    - rehash ircds
  handlers:
    - name: rehash ircds
      action: shell pkill -HUP -F ~/shadowircd/etc/ircd.pid

